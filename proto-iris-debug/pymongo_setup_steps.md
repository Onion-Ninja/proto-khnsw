## This shows steps to setup pymongo for this repo

Activated the required conda env (alam) in this case

Ran the following:
```bash
pip install pymongo bson
```

Copied the required files from ~/np/iris_db to /home/nishkal/dd/k-proto-hnsw/proto-iris-debug
```bash
cp ~/np/iris_db/biolab.py .
cp ~/np/iris_db/lib.py .
```

